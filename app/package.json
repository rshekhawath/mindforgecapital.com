{
  "name": "mindforge-capital-app",
  "version": "1.4.0",
  "private": true,
  "description": "MindForge Capital member app — installable PWA + Capacitor iOS/Android shell. View your strategy picks and link your broker.",
  "scripts": {
    "serve": "python3 -m http.server 8080 --directory www",
    "cap:sync": "cap sync",
    "ios:add": "cap add ios",
    "ios:open": "cap sync ios && cap open ios",
    "android:add": "cap add android",
    "android:open": "cap sync android && cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0",
    "@capacitor/ios": "^6.1.0",
    "@capacitor/android": "^6.1.0"
  }
}
